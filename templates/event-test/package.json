{
  "name": "event-test",
  "private": true,
  "scripts": {
    "test": "sls package -r us-east-1",
    "dp:lcl": "sls deploy -v -r us-east-1 --force",
    "rm:lcl": "sls remove -r us-east-1"
  },
  "devDependencies": {
    "serverless": "^3.21.0"
  },
  "dependencies": {
    "uuid": "^7.0.2"
  }
}
